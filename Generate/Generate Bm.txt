javascript:(function () {function run () {	var quText = "", ansText = "";	function addQu (qu) {		quText += "\"" + proc (qu) + "\",\n";		count += 1; if (count % 5 == 0) quText += "\n";	}	function addAns (ans) {		ansText += "\"" + proc (ans) + "\",\n";		if (count % 5 == 0) ansText += "\n";	}	function replaceAll (find, replace, str) {		return str.replace(new RegExp(find, 'g'), replace);	}	function proc (iText) {		text = iText;		text = replaceAll ("\n", "\\n", text);		text = replaceAll ("\"", "\\\"", text);		text = replaceAll ("<", "&lt;", text);		text = replaceAll (">", "&gt;", text);		return text;	}	ys = document.getElementsByClassName("quia_standard");	var al = 0; var count = 0; var match = 0;	var quProc;	for (var i = 0; i < ys.length; ++i) {		if (ys [i].tagName == "SPAN") {			quRaw = ys [i].innerHTML;			if (quRaw.indexOf ("<") > 0) {				quProc = quRaw.substr (0, quRaw.indexOf ("<br "));				al = 1;			} else {				quProc = quRaw;				al = 2;			}		}		if (ys [i].tagName == "TD" && ys[i].getElementsByTagName ("span").length == 0) {			if (al == 1) {				fnt = ys [i].getElementsByTagName ("font");				for (var j = 0; j < fnt.length; ++j) {					ansRaw = fnt [j].innerHTML;					if (ansRaw.indexOf (" (correct answer, your response)") >= 0) {						ansProc = ansRaw.substr (0, ansRaw.indexOf (" (correct answer, your response)"));						addQu (quProc);						addAns (ansProc);					}				}			}			if (al == 2) {				fnt = ys [i].getElementsByTagName ("font");				if (fnt.length > 0) {					ansProc = ys [i - 2].innerHTML;					addQu (quProc);					addAns (ansProc);				}			}		}	}	console.log (quText);	console.log (ansText);	var text = "";	if (text == "") {		text += "que = [<br>";		text += replaceAll ("\n", "<br>", quText);		text += "<br>];<br>ans = [<br>";		text += replaceAll ("\n", "<br>", ansText);		text += "<br>];<br>run ();<br>";	} else {		text += "que = [";		text += replaceAll ("\n", "", quText);		text += "];ans = [";		text += replaceAll ("\n", "", ansText);		text += "];run ();";	}	text += "";	window.open("", "Econ Quia", "width = 800, height = 800").document.write (text);}run ();}) ();